# 2026-04-11

- SF requested that future conversations should be in Chinese.
- Key preference: after important conversations or multi-step tasks, record the key discussion points and actions taken in daily memory to reduce loss from future context compression.
- Checked current Docker-visible multi-agent workspace paths in `~/.openclaw/openclaw.json`:
  - main → `/home/node/.openclaw/workspace`
  - dev → `/home/node/.openclaw/workspace-dev`
  - content → `/home/node/.openclaw/workspace-content`
  - ops → `/home/node/.openclaw/workspace-ops`
  - law → `/home/node/.openclaw/workspace-law`
  - finance → `/home/node/.openclaw/workspace-finance`
- User noted they manually corrected workspace paths because OpenClaw is running inside Docker, so container-visible paths must be used.
- Verified current multi-agent state:
  - Only `/home/node/.openclaw/workspace` existed initially.
  - Missing directories were: `/home/node/.openclaw/workspace-dev`, `/home/node/.openclaw/workspace-content`, `/home/node/.openclaw/workspace-ops`, `/home/node/.openclaw/workspace-law`, `/home/node/.openclaw/workspace-finance`.
  - `openclaw status --deep` showed Feishu channel configured with 6/6 accounts enabled.
  - `openclaw channels list` showed Feishu accounts `main/dev/content/ops/law/finance` all configured and enabled.
  - Current Feishu health still reported `400` on deep probe, despite channel status showing configured.
  - Current config has `channels.feishu.dmPolicy = "open"`, so DM pairing is effectively not required for private chats at the moment.
- User confirmed by testing in Feishu that the other 5 bots can already be used successfully.
- Created missing workspace directories:
  - `/home/node/.openclaw/workspace-dev`
  - `/home/node/.openclaw/workspace-content`
  - `/home/node/.openclaw/workspace-ops`
  - `/home/node/.openclaw/workspace-law`
  - `/home/node/.openclaw/workspace-finance`
- Verified that all 6 workspace directories now exist.
- Recorded Docker host/container path mapping to avoid future workspace path mistakes:
  - Host base path (macOS): `/Users/sf/projects/AI_code/openclaw/docker_openclaw/data/openclaw/.openclaw`
  - Container base path: `/home/node/.openclaw`
  - Mapping examples:
    - Host `.../.openclaw/workspace` ↔ Container `/home/node/.openclaw/workspace`
    - Host `.../.openclaw/workspace-dev` ↔ Container `/home/node/.openclaw/workspace-dev`
    - Host `.../.openclaw/workspace-content` ↔ Container `/home/node/.openclaw/workspace-content`
    - Host `.../.openclaw/workspace-ops` ↔ Container `/home/node/.openclaw/workspace-ops`
    - Host `.../.openclaw/workspace-law` ↔ Container `/home/node/.openclaw/workspace-law`
    - Host `.../.openclaw/workspace-finance` ↔ Container `/home/node/.openclaw/workspace-finance`
  - Lesson: when OpenClaw runs in Docker, `agents.list[].workspace` must use container-visible paths, not host `/Users/...` paths.
- Updated `AGENTS.md` across main + all specialized workspaces to a shared OPC team directory, so every agent knows teammate roles and should use `sessions_send` for cross-agent collaboration.
- Optimized initialization for main + specialized agents by standardizing `USER.md` with the concise SF profile (`Name: SF`, `时区: UTC+8`, `备注: 业余AI开发爱好者`) and refining specialized `SOUL.md` files with clearer role boundaries and cross-agent collaboration hints.
- Further specialized the five non-main agent personas by refining their `SOUL.md` files for realistic use cases: `dev` (coding/deployment/debugging), `content` (公众号/文案/内容创作), `ops` (增长/转化/分发), `law` (合同/合规/风险提示), `finance` (成本/预算/财务分析).
- Corrected team roster documentation so the main coordinator agent is consistently named `main (Jarvis 🤖)` rather than the previous alternate label.
- Added a root-level `README.md` for the unified `/home/node/.openclaw` repository, documenting repo purpose, multi-agent structure, Docker host/container path mapping, git boundaries, and what should or should not be version-controlled.
- Corrected `channels.feishu.accounts.ops.appId` from the mistaken `Acli_a950b267aab91bca` to the confirmed value `cli_a950b267aab91bca`, after narrowing a `probe failed` issue to the ops bot only.
- Added a practical maintenance SOP to the root `README.md`, covering: adding a new agent, adding a Feishu bot account, checking Docker workspace paths, pre-push git self-checks, and single-account Feishu probe troubleshooting.
- Further improved specialized agent initialization by replacing generic `TOOLS.md` files in `dev/content/ops/law/finance` with role-specific templates, so each agent knows what kind of durable environment notes and operational lessons should be stored there.
- Added clearer initialization notes to each specialized agent's `memory/2026-04-11.md`, including role, collaboration hints, and what kinds of durable lessons belong in that workspace.
- Recorded a durable collaboration rule: future multi-step or cross-functional tasks should be assigned to main (Jarvis) first, and then Jarvis should distribute work to other agents.
- Real-world group collaboration test exposed an important issue: multiple bots in the same Feishu group may each react to the same prompt independently, but Jarvis-first orchestration is not yet reliably enforced. Several agents reported they could not find peer sessions; one (`ops`) eventually produced a synthesized result instead of a clean Jarvis-led routed workflow. Future improvement should focus on making `main/Jarvis` the clear orchestration entry point for group tasks and reducing parallel unsolicited bot replies in shared groups.
- Added a Feishu group peer binding for chat `oc_0c9c940f206345acbb92b382077c2f37` so this collaboration group routes to `main` (Jarvis) with higher priority than account-level bindings.
- Refined non-main agent collaboration rules so in shared group chats, cross-functional tasks should default to Jarvis orchestration unless an agent is explicitly asked to handle a narrow subtask.
- Replaced the earlier “group-direct multi-bot collaboration” idea with a more stable workflow: future cross-agent tasks should be sent to Jarvis in private first; Jarvis then orchestrates other agents in the background and posts the unified result back to the target group.
